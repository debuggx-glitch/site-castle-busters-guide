import type {MetadataRoute} from "next";import {guides} from "@/lib/guides";import {site} from "@/lib/site";export const dynamic="force-static";export default function sitemap():MetadataRoute.Sitemap{const staticPages=["","/about/","/privacy/","/contact/"].map((path)=>({url:`${site.url}${path}`,lastModified:"2026-09-04",changeFrequency:"weekly" as const,priority:path?0.8:1}));const guidePages=guides.map((guide)=>({url:`${site.url}/guides/${guide.slug}/`,lastModified:new Date(`${guide.updated} UTC`),changeFrequency:"weekly" as const,priority:0.8}));return [...staticPages,...guidePages]}
